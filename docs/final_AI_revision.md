# you asked

message time: 2026-07-19 12:45:42

Continue

---

# claude response

The audit is complete. I've read every line of the four LaTeX sources and the bibliography, re-derived the τ design chain, the stability coefficients and all six robustness estimates, and recomputed every numerical claim in the discussion sections against the tables (roughly sixty checks; I'll spare you the ones that passed except where they matter). Below is the full report. Where a finding depends on something I cannot see, such as the Gridap driver, I say so explicitly.

## Coverage and headline assessment

What I verified and found correct, so you know the ground covered: the dimensionless form and the f-scaling footnote; all three τ asymptotic limits and the σ̃ expression; the Fourier assembly into C_α, including the factor 5 and the λ-weighting; the stability coefficient algebra of Lemma 5.1 including the amended conditions c₁ > 2ξC²ᵢₙᵥ, ξ > 2; the ε_ref chain in §7.2 (both inequalities); all six robustness estimates in §6 including the amended prefactors α₀^(−1/2), Da^(1/2)/(α₀α_∞)^(1/2) and the h-independent Da^(−1/2) mitigation; the appendix–main-text consistency of B_S, the consistency argument, and the Neumann trace with the amended factor 2; the Kuhn threshold (100+5√2)/24 ≈ 4.46; the 15 = 18−3 case count; Da_h ≈ 9.8 and the 20× plateau factor; the √6 nodal-versus-best-L² factor; and essentially every quantitative claim in the amended §7.1 paragraphs, including the 5.0/11.6 interpolation growth factors, the 1.3–2.0 and 3.5–4.1 relative-error growth ranges, the 8.3–12.4 and 10.2–18.8 pressure factors, and the reaction-limit numbers 8.76e-6, 1.62e-5, 1.65×, 1.85. The paper is in genuinely strong shape and the amended discussion is unusually honest about one-sided bounds. The issues below are what survived an adversarial pass.

## Substantive findings

**1. Sign error in eq. (weak_form_eliminated_subscales), line ~516.** The equation reads B(u,U_h,V_h) − ⟨𝓛𝓛̃⁻¹𝓡U_h, V_h⟩ = L(V_h). With your own conventions 𝓡 := F − 𝓛U and Ũ = 𝓛̃⁻¹(𝓡U_h), the scale splitting gives B(u,U_h,V_h) + B(u,Ũ,V_h) = L(V_h) and B(u,Ũ,V_h) = ⟨V_h, 𝓛Ũ⟩ plus the Neumann trace, so the sign must be **plus** — as it correctly is in the final method (3.14), which adds +⟨𝓛*V_h, τΠ̃[𝓡U_h]⟩. The error is motivational only and does not propagate, but it is a genuine typo, and it sits in exactly the paragraph a careful reader uses to orient themselves.

**2. Missing factor 2 in the elemental-matrices appendix.** In eq. (StabilizationLVLU) the terms νΠ̃∇v∇β and νΠ̃∇u∇β should both be 2νΠ̃∇v∇β and 2νΠ̃∇u∇β; the same in the test slot of eq. (StabilizationLVF). Derivation: 2∇·(ανΠ∇u) = α[ν∆̄u + 2ν(Π∇u)∇β] once ∇α = α∇β is pulled out, and your ∆̄ correctly carries its own 2s. I checked the assembled matrix entries later in the same appendix (A_{Gβ}, A_{Dβ}, G_{βF}, D_{βF}, and friends) and they are all consistent with the **correct** factor 2 — only the two compact displays are wrong. So the implementation reference is fine; the displays need the 2s.

**3. Damköhler inconsistency in §7.3 — this one needs a decision.** The text claims the Damköhler number "is set solely by the minimum porosity through Da(α₀) = C_a((1−α₀)/α₀)² + C_b((1−α₀)/α₀) ≈ 2 and 40", and that removing the reference's explicit 1/Re factor means "the Reynolds and Damköhler numbers can be varied independently". But by the paper's own definition (6.1), Da = σL²/(α_∞ν) with ν = 1/Re under U = L = 1. If σ as implemented carries no ν factor (as eq. (DBFResistanceTerm) is printed), then at Re = 10⁵ the actual Da is 2×10⁵ and 4×10⁶, not 2 and 40 — and, ironically, it is the *reference's* 1/Re factor that makes the linear-drag Da Re-independent, so the sentence has it backwards. Two possible resolutions: (a) if the driver implements σ = ν(a + b|u|), then Da is indeed Re-independent and equals the quoted formula, but then eq. (DBFResistanceTerm) must show the ν, and you should note that C_b = 1.75 then retains Ergun's value while losing the inertial (ν-free) Forchheimer scaling; or (b) if the driver implements the ν-free σ, rewrite the paragraph in terms of a drag level σ₀(α₀) := a(α₀) + b(α₀) ≈ 2 and 40, note Da = σ₀Re, and describe the four corners as (Re, σ₀) combinations. Please check the Gridap driver; the text as printed underdetermines σ and contradicts (6.1) either way. This also feeds finding 13.

**4. "Optimal" misused for superoptimal pre-asymptotic slopes, in three places.** (a) Line ~1156, the fold paragraph: "converges at the optimal rates (for P1, 2.99–3.03 in the L²-norm…)". The P1 L² optimum is 2; a slope of 3 is a pre-asymptotic artifact — the same phenomenon your DBF caption correctly labels "two-mesh estimates in a pre-asymptotic range". Say "at or above the optimal rates" or "at superoptimal pre-asymptotic slopes". (b) §7.3, "recovers accurate, optimally-converging solutions" at (10⁵, 0.1), immediately before the sentence that correctly hedges the ≈3 slopes as pre-asymptotic — the two sentences contradict each other. (c) §7.2, "the P2 velocity attains essentially optimal L² and H¹ orders" for slopes 3.33/2.22 against optima 3/2. Here you have a strictly stronger and cleaner claim available: the observed slopes **match the interpolant's** (3.20/2.22 on the same sequence), whose own superoptimal values reflect the short sequence — the regular P2 slope comes from N = 20→24, an h-ratio of 1.2 that amplifies noise in a two-mesh slope by 1/log(1.2) ≈ 5.5×. "Matches the interpolation reference" is both true and more informative than "essentially optimal".

**5. The quadrature-excess claim at line ~1170 is true only under an unstated definition.** "The excess… absolute size agrees to within 1% at α₀ = 0.5 and 0.05" holds in quadrature: √(0.123² − 0.0352²) = 0.1179 versus √(0.212² − 0.177²) = 0.1167, ratio 1.010. A reader computing the naive difference gets 8.8e-2 versus 3.5e-2 — a factor 2.5 apart — and will conclude the sentence is false. Define the excess explicitly as (e²_OSGS − e²_ASGS)^(1/2), which is also the defensible notion if the extra error component is roughly orthogonal to the shared one; one clause fixes it.

**6. "Three significant digits" overclaims for Q2.** At α₀ = 0.5 the Q2 H¹ velocity is 4.29e-4 against interpolation 4.28e-4 — the third digit differs (0.23%). The sentence attaches the three-digit claim to both elements. Scope it to P1, or write "to within a fraction of a percent at α₀ = 0.5". Relatedly, the text quotes 3.499e-2 where the table shows 3.50e-2; the reader cannot verify the extra digit, so either add it to the table or drop it from the text.

**7. "Their projection is exactly zero" (line ~642) says the opposite of what is meant.** Since σu_h lies in the FE space, its Π_h-projection equals *itself*; what vanishes is the fluctuation (I − Π_h)(σu_h). Within the same paragraph "projection" has just been used for π_h, so the sentence is wrong as literally read. Rephrase: "they are annihilated by I − Π_h, so their contribution to 𝓡 − π_h vanishes identically". Same slip in the footnote at ~614: an unconstrained projection space would force boundary DOFs to *zero* (the space is homogeneous), not to "mirror the prescribed Dirichlet data".

**8. Missing Fourier appendix.** Line 794 promises a term-by-term derivation in app:FourierTau and line 1696 does \input{fourier_appendix.tex}, but the file is not among the uploads and the label is undefined in what I have. If it exists locally, I simply couldn't audit it — send it if you want it checked; if it doesn't, this is a blocking gap since an amendment explicitly advertises it. (Also: \externaldocument{supplement} — confirm a supplement is actually planned, otherwise drop the line.)

**9. The delimitation paragraph (~1102) misses one theory–practice gap, and two reproducibility items are absent.** The paragraph is excellent, but the simulations use pointwise-variable τ within elements (admitted at ~832) while every proof requires elementwise-constant τ (Step 6b's integration by parts, the jump machinery). Add it to the list. Second: §7.3 states α is nodally interpolated, but §7.1–7.2 never say whether α and ∇α are evaluated exactly at quadrature points or interpolated — and the conclusions (~1677) lean on "our numerical experiments indicate that the model error introduced by the interpolation of α does not spoil convergence", which currently rests on §7.3 alone unless you say otherwise. Third: nonlinear stopping tolerances are never reported, yet FMEs are compared to interpolation references at the third significant digit; one sentence fixes it. No code/data availability statement either — SIAM increasingly expects one, and a Gridap driver is eminently shareable.

**10. The §7.3 closing attribution (~1656) is below the paper's own standard.** "The loss of accuracy… is consistent with the α₀^(−1/2) dependence": observed pressure growth is 6.3–6.4×, exceeding both α₀^(−1/2) = 2.24 and even the uniform 1/α₀ = 5, and the pressure interpolation reference is α₀-independent, so interpolation growth cannot absorb the excess as it does for velocity (where 3.5 ≤ 3.46 × 2.24 works out). The deeper problem is that the corners conflate α₀ with Da (2 → 40, per finding 3), so the comparison compounds the porosity weight with a change of regime and cannot isolate anything. Everywhere else the paper is scrupulous about one-sidedness; rewrite this sentence in that spirit: the observed factors are consistent with the product of interpolation growth and the estimated prefactors, but the design does not isolate the α₀ dependence.

**11. The 3D irregular-mesh attribution ignores the paper's own strongest evidence.** The sub-optimality is blamed on "the element-quality tail", an unverified diagnosis — under bounded-shape red refinement, quality degrades constants, not rates. The stronger, in-table argument is that the **interpolant shows the same depressed rates** on the same meshes (P1: 1.83 L², 0.71 H¹; P2: 2.67/1.52), so the defensible claim is "shared by the interpolant, hence a property of the mesh sequence, not of the formulation". Point at those columns. In the same section, one honest sentence is owed on the direction reversal versus 2D: in 3D the OSGS pressure has better rates but **larger** errors than ASGS at these resolutions (regular P1 L²: 1.85/4.55e-2 versus 1.37/2.87e-2), and the footnote admits the OSGS H¹ pressure "barely converges" (the 1.29 saturation). The conclusions' variant comparison needs the matching caveat (see below).

**12. The Taylor–Hood pressure explanation overreaches.** "The P2/P1 pair is inf-sup stable — so that its pressure still attains the optimal L² rate": standard estimates pollute the pressure by the velocity error, which here is O(1) and non-convergent, so inf-sup alone does not explain a pressure sitting *exactly* on the P1 interpolation floor (9.84e-6) next to a garbage velocity. The observation is striking and worth keeping; either soften the causal "so that" to "remarkably", or explain the mechanism if you have one.

**13. Function-space slip.** V₀ := H₀¹(Ω)^d imposes zero trace on all of ∂Ω, yet the weak form retains Γ_N pairings; it should be {v ∈ H¹ : v|_{Γ_D} = 0}. Inconsequential for the analysis (all-Dirichlet), but SIAM referees notice. The H^(−1/2)(Γ_N) trace wording nearby is also informal.

**14. Smaller alignment items, batched.** Line ~1158 "the one appreciable exception" (singular) conflicts with ~1170's "the only other appreciable one"; say "the main exception", and optionally note the 1.9× P1-L² factor flips direction between rows, i.e. is not one-signed. "The strongly reaction-dominated column" should be "rows" or "regime". The step Π̃[τ⁻¹Ũ] = τ⁻¹Ũ at ~560 is strictly ≈ under either orthogonality convention; your own footnote defines ≈ for exactly this purpose. The λ of the Λ-weighting collides with the generic eigenvalue λ in spec_{Λ⁻¹} a few lines later; rename one (μ for the eigenvalue). The scalar velocity scale U collides with U = [u; p]; consider U₀, or at least keep them out of the same display. \sin is typeset as \text{sin} at ~1118. The amendcolor comment says "dark green" while the RGB (0.58, 0, 0.83) is violet.

## Reorganisation of the numerical-results discussion (§7.1, echoed in §7.2–7.3)

The current sequence — fold paragraph, overview, interpolation-reference methodology, α₀ dependence, then three regimes — buries three threads (variant comparison, porosity dependence, reference framing) across seven dense paragraphs. Same ground, better guidance:

**A. How to read the tables.** One paragraph stating the three questions the tables answer (rates versus theory; errors versus the interpolation reference; ASGS versus OSGS), defining the interpolation reference once in the text, with the √6 nodal-versus-best-L² caveat and the reminder that the §6 estimates are one-sided upper bounds. This also unburdens the first table caption, which currently carries global conventions for all four tables.

**B. Headline.** One short paragraph: wherever no single term dominates, both variants sit on the interpolant (three digits for P1, a fraction of a percent otherwise) — the stabilization costs nothing measurable. This is the paper's most striking empirical result and currently arrives mid-paragraph.

**C. Regime by regime**, with \paragraph headers and a *fixed internal order*: velocity rate → velocity error versus floor → pressure rate → pressure error versus floor → which §6 estimate it matches. The reader then knows where to look for anything.

**D. Effect of the porosity level.** Consolidate every α₀ thread here — the inherited solution scaling, the P1-L² residual method effect, the pressure factors, the reaction-limit 1.85, the porosity-independence of the OSGS excess — ending with the Remark 6.1 cross-reference on weighted versus uniform bounds.

**E. ASGS versus OSGS.** The two findings in one home: pressure order for k ≥ 2 (OSGS better, with the mechanism) and the reaction-dominated P1 velocity gap (OSGS worse, pre-asymptotic evidence, explicitly open). Currently these are scattered across four paragraphs.

**F. Anomalies and limits of the sequence.** The fold (the long paragraph at ~1156, condensed, moved here or to a table footnote plus a pointer) and the Q2 convection pre-asymptotics.

Echo the same C-order in §7.2 and §7.3. Two further suggestions: the section is eight tables plus one porosity figure and no plots — a single log-log figure (velocity and pressure error versus h for three representative regimes, interpolant dashed) would make B visually immediate, and the supplement machinery is already wired if you want to move two of the four 2D tables there. Finally, promote the delimitation paragraph to a titled remark ("Scope of the comparison") so it is findable and citable.

## Introduction and abstract

The abstract undersells the paper badly: it reads as methods-only, with no mention of the stability and porosity-weighted a priori analysis robust in Re and Da, the 3D campaign, or the Taylor–Hood/DBF comparison. Those are the contributions a SIAM reader scans for; rewrite it around them.

Correctness items: "the only precedent of a stabilized finite element method: the Local Projection Stabilization" is an unhedged universal claim sitting in tension with your own nillama2022explicit discussion; add "to our knowledge" and scope it to variable porosity. The advantages list omits your best card — residual-based stabilization simultaneously cures convection-dominance, which inf-sup pairs alone do not, *as your own §7.3 demonstrates with the non-convergent TH velocity at Re = 10⁵*; add it and forward-reference. The commented-out sentence at ~222 citing the stabilized-Darcy literature (Masud, Juanes, Codina 2015, Braack — all still in the .bib) should be reinstated in some form: the intro currently cites none of that school, and its members referee these papers. Grammar: "concentrate in" → "concentrate on"; "no need for revisiting the theory was required" is doubly negated — "no revision of the theory was required"; "associated to" → "associated with" (twice); "(commutative)" projections → "commuting"; "Π^S = ∇^S u" conflates operator and action (twice, ~266–268); "in its most recent form" leaves ambiguous whether it modifies the argument or the NS system. The AMS codes need fixing: 65M60 is time-dependent FEM and this paper is stationary — replace with 76S05 (flows in porous media, currently absent and the most relevant code on the list) and consider 76M10 and 65N15. Keywords: add "porous media"; you list OSGS but not ASGS.

## Conclusions

Four gaps and one calque. "In front of extreme variations" is a Catalan/Spanish calque — "under" or "in the face of". "Just as robust… as it is well-established to be" overstates given the excluded (10⁶, 0.05) corner; one clause acknowledging the coarse-mesh solvability limit keeps you honest. The conclusions omit the headline empirical finding (errors sitting on the interpolant in non-dominated regimes, with low-α₀ degradation inherited from the exact solution) and omit the porosity-weighted α_K estimate as a named theoretical contribution — both belong here. "The two variants behave very similarly for the velocity" needs the documented exception (the reaction-dominated P1 gap the results section calls the largest discrepancy of the campaign and explicitly leaves unresolved), and "somewhat better pressure convergence" deserves the 3D caveat from finding 11. Lastly, "the absolute errors are very stable… when the convergence rate is similar" is vague; the interpolation-reference framing gives you a sharp version of the same sentence.

## Obvious weaknesses (referee's-eye view)

The analysis covers only the linearized, constant-σ, all-Dirichlet, ASGS case — honestly stated in the delimitation paragraph, but the abstract and conclusions must not outrun it, per the items above. The ASGS pressure is one order suboptimal with no OSGS theory, despite numerics that beg for it. The campaign is purely manufactured, from a single trig-times-bump family; one non-manufactured qualitative example (flow past the porous bump without forcing) would be cheap and would defuse the predictable "engineering relevance" objection given the intro's applications framing. Rates from two-mesh slopes are fragile, fatally so for regular 3D P2 (h-ratio 1.2); least-squares fits over the last three or more meshes would be more robust, or at least acknowledge the fragility. There is no computational-cost comparison, either between variants (OSGS pays for projections) or against Taylor–Hood, which weakens §7.3's practical conclusion. The fold is reported but unanalysed. And findings 3 and 8 must be resolved before submission.

## Future research

Beyond the paper's own list (term-by-term stabilization; steep or discontinuous α via integrating the ∇α terms by parts; interpolated-α analysis; transient porosity, which I know is underway): first, an OSGS analysis proving the observed L²-pressure optimality for k ≥ 2 — the mechanism you already sketch (the orthogonal projection removing the resolvable residual, in the codina2008analysis mould) is theory-shaped and provable. Second, the P1 OSGS reaction-dominated velocity gap: a dispersion or error-equation analysis of the lagged-projection iteration in the reaction limit would resolve the campaign's one open question. Third — and this is the experiment I would most urge — a *discriminating* test for bound sharpness: place the interpolation-error mass outside the low-α plateau (an oscillatory solution component supported where α = 1). The weighted bound then predicts no α₀ loss while the uniform bound permits 1/α₀, converting "the experiments cannot select between the bounds" into "they can", for the cost of one manufactured solution. Fourth, nonlinear discrete existence and uniqueness in the Brezzi–Rappaz–Raviart frame, which would formalize the fold as loss of the small-data condition and make the continuation-in-h behaviour a result rather than an anecdote. Fifth, finish the Kuhn-tetrahedron inverse-constant companion the paper promises will be "reported separately" — you already hold that material. Sixth, pressure-robust variants (H(div) post-processing) are natural given the τ₁∇p contamination channel your reaction-limit analysis exposes; and anisotropic σ tensors would exercise the ρ_{Λ⁻¹} machinery the design already supports.

## Mechanical pre-submission checklist

154 \amend, 12 \Guillermo and 3 \Joaquin markers are live and will ship in colour; the flattening mechanism exists in the preamble — use it. fourier_appendix.tex missing (finding 8). All cited keys resolve; the .bib carries ~17 unused template leftovers (AMSMSC2010, CalcI, clawpack, siam, …) worth pruning, and Codina2015OnSM lacks a booktitle should you reinstate the Darcy sentence; DOIs are absent throughout, which SIAM tolerates but likes. One \eqref amid uniform \cref (line ~994). Codina and de-Pouplana have no email addresses in the author block. "Spatially-inhomogeneous" should drop the hyphen; "such term" → "such a term"; "applied and further developed to" → "extended to"; the sentence at ~832 ("since, given that…, it is only important that") wants rewriting; the footnote at ~226 is a fragment.

If you send me fourier_appendix.tex and confirm which σ the DBF driver implements, I can close findings 3 and 8; everything else is actionable from the text alone.

---
Powered by [AI Exporter](https://saveai.net)