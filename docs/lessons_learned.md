# Lessons Learned – Porous VMS Solver

This file is the authoritative, persistent memory bank for historical mistakes, anti-patterns, and
mathematical regressions previously corrected in the Porous Navier-Stokes solver.

**Rules for this file:**
* **append-only content** — never edit or delete an existing row; a later reversal/resolution is a NEW row
  that references the one it supersedes (see the c₁ saga in §6 for the canonical example)
* one row per lesson; concise but precise; no vague wording
* rows are grouped into the thematic sections below — **append a new row to the section it best fits**
  (add a new section if none does); within a section rows stay in date order
* "Why it was wrong" must be mathematical or numerical, not emotional
* "Correct action taken" must describe the concrete fix or policy
* include tags like `ASGS`, `OSGS`, `adjoint-sign`, `Jacobian`, `Newton`, `MMS`, `projection`,
  `stopping-criteria`, `legacy-branch`, `verification`, `3D`, `c1`, `encoding`, `config-strictness`

> Reorganized into sections 2026-07-09 for navigability; **no existing row's content was altered** —
> only regrouped and date-ordered. Five distilled lessons were added (the 3D-P2 resolution, the JFNK
> P2-3D preconditioner fix, the JFNK-for-CocquetFormMMS enablement, the high-Re eps-ladder gotcha, and
> the k≥2 convergence-gate rule).


## 1. Formulation & stabilization invariants

The continuous-VMS math contracts — get these wrong and coercivity/consistency break at the extremes.

| Date | Error / Anti-pattern | Why it was wrong | Correct action taken | Related files | Tags |
| ---- | -------------------- | ---------------- | -------------------- | ------------- | ---- |
| 2026-04-13 | Replacing canonical viscous operator formulations silently with simplified legacy mapping | Fails to account for non-constant viscosity distributions resulting in invalid Laplacian simplifications $\nabla \cdot (\nu \nabla u)$ rather than $\nabla \cdot (2\nu \nabla^s u)$. | Enforced analytical deviatoric mapping $\varepsilon(u)$ evaluation for strictly valid momentum transport. | `viscous_operators.jl` | `ASGS`, `legacy-branch` |
| 2026-04-13 | Reversing the adjoint coefficient sign in stabilization evaluation | Flips the mathematical properties natively switching the continuous VMS bounds from bounded diffusion to exponentially anti-diffusive state explosions. | Adjoint symmetry explicitly evaluated across standard `c_1` and `c_2` mapped bounds dynamically in code variables. | `reaction.jl`, `tau.jl` | `adjoint-sign`, `Jacobian` |
| 2026-05-21 | Feeding the τ velocity-floor (`effective_speed`) into the physical Forchheimer reaction speed | `effective_speed` carries a mesh-dependent diffusive floor `u_base + h_floor_weight·c₁ν/(c₂h)` that scales like ν/h and **grows** under refinement. That is acceptable inside τ (a stabilization parameter that multiplies a vanishing strong residual) but, fed into the drag `σ=a(α)+b(α)\|u\|`, it makes each mesh solve a different effective drag law. For the Cocquet benchmark (Re=500, Forchheimer, h_floor_weight=0.1) the floor reached 0.128 at the N=80 reference — larger than \|u\|_max≈0.125 — collapsing velocity L² convergence to ~0.14–0.53 (paper and MMS both give ≥2). Invisible to: pure NS (σ=0), constant-σ MMS (speed unused), and any Forchheimer MMS run with `h_floor_weight=0` (where `reaction_speed≡effective_speed`). | Introduced `reaction_speed` (constant `u_base_floor_ref` floor only, mesh-independent) for the physical reaction; `SigOp`/`DSigOp` use it while τ keeps `effective_speed`. Cocquet config sets `h_floor_weight=0`. Verified: Cocquet velocity H¹ slopes 1.94/2.03 (optimal), L² 2.80/2.32 (was 0.14/0.53); constant-σ MMS unchanged. Diagnosed via a new manufactured solution with the *identical* Forchheimer+SymmetricGradient operator (`test/extended/CocquetFormMMS`), which proved the formulation itself was optimal and the source paper reports O(h²) at the same parameters. | `regularization.jl`, `continuous_problem.jl` (`SigOp`/`DSigOp`), `mms_paper_2d.jl`, `CocquetTubeTest/data/structured/test_config.json` | `reaction`, `Forchheimer`, `regularization`, `MMS`, `verification` |
| 2026-07-12 | Paper appendix "Putting together the results…" assembled `K`/`F` with **four broken symbols** — `\mathbf{I}` (undefined), bare `\mathbf{G}_β`/`\mathbf{D}_β` (undefined) in the Galerkin `(V,U)` block, and orphaned `\mathbf{V}_T` (defined, never assembled). Missed by the entire 106-check sympy suite. | The verification only ever checked each elemental matrix **in isolation** — component formulas (`elemental_matrices_verification.py`) and family-sums vs. the bilinear form (`elemental_bilinear_form_verification.py`) — and **never looked at the assembled display**. So a matrix can be individually correct yet named-without-definition or defined-then-dropped. Mathematically: `I` is the transient mass matrix (=0 in this stationary paper); the Galerkin viscous term `2ν(α∇v,∇ˢu)−(2ν/3)(α∇·v,∇·u)` differentiates to **exactly** `G_S+D_νD` (α is a fixed factor, no IBP on it ⇒ no `∇α`), so bare `G_β,D_β` are spurious, not typos; dropping `V_T` silently discards the Neumann load. All four independently re-confirmed by adversarial reviewers; the ~60-term content checks stayed green (defect is display-only, invalidates no theorem). | Removed the `[[I,0],[0,0]]` block and bare `G_β,D_β`; restored `V_T` in `F` (marked `\amend`); `latexmk` clean. Added the durable structural check `assembly_consistency_verification.py` (wired into `run_all.py`): *every matrix named in the assembly must be defined and every defined matrix must be used*. Pre-fix it reports exactly `{I,G_β,D_β}` undefined + `{V_T}` unused; post-fix green. Suite now **110/110 across 9 scripts**. Component definitions were **correct and left untouched**. Full write-up: `docs/part_i_erratum.md`. | `theory/paper/elemental_matrices_appendix.tex`, `proof_verification/sympy/assembly_consistency_verification.py`, `proof_verification/sympy/README.md`, `docs/part_i_erratum.md` | `paper`, `appendix`, `assembly`, `verification-gap`, `durable-check`, `stationary`, `Neumann`, `porosity-gradient` |

## 2. OSGS solve, projection & coupled route

The staggered projection, the coupled-only inexact Newton, and the ways OSGS silently degenerates to ASGS.

| Date | Error / Anti-pattern | Why it was wrong | Correct action taken | Related files | Tags |
| ---- | -------------------- | ---------------- | -------------------- | ------------- | ---- |
| 2026-04-13 | Using raw infinity norm for staggered OSGS state drift | $\ell^\infty$ drift relies on interleaved DOF layouts, heavily skewing multi-physics (velocity vs pressure) scaling and corrupting projection limits. | Replaced discrete array slicing with continuous functional $L^2$ mapping via identical finite element operator `∫(e_u ⋅ e_u)dΩ`. | `porous_solver.jl` | `OSGS`, `stopping-criteria`, `projection` |
| 2026-04-13 | Passing implicit or unbracketed projection state during stabilization initialization | Passing zeroed residuals into an iterative OSGS staggered loop falsely mimics ASGS projection-free boundaries, corrupting algorithmic continuity. | OSGS natively bootstraps prior iteration $\Pi(\mathcal{R})$ before starting the Newton sub-solves inside the discrete loop. | `porous_solver.jl` | `OSGS`, `ASGS`, `projection` |
| 2026-04-13 | Coupling outer OSGS tolerance checks loosely against static inner Newton boundaries | Allows the inner solver to over-iterate wildly past floating point boundaries when the outer projection drift converges much earlier, exploding iteration costs. | Implemented dynamic Eisenstat-Walker coupling `tau_inner_m = max(ftol, min(0.5, 0.1 * prev_osgs_drift))`. | `porous_solver.jl` | `OSGS`, `stopping-criteria`, `Newton` |
| 2026-05-19 | Refactoring `solve_system` monolith into helpers without preserving function-level `success` carry-over | Original `solve_system` set `success = true` after Stage I and the OSGS outer loop only flipped it on explicit convergence/failure events. Naively initialising a fresh local `success = false` inside the extracted `_run_osgs_relaxation!` helper silently changed semantics for the "Stage I OK + OSGS budget exhausted without explicit failure" path — the `osgs_orthogonality_quick_test` flipped from pass to fail despite byte-identical OSGS iter-by-iter numerics. The bug is invisible to blitz tests; only the quick gate caught it. | Extracted helper now takes `initial_success` as an explicit argument and seeds its local `success` with the caller's value. Carry-over semantics documented in the helper's docstring. Identical behaviour-preservation gates (h5diff vs pre-refactor) made the symptom isolate cleanly. | `porous_solver.jl` (`_run_osgs_relaxation!`) | `OSGS`, `refactoring`, `stopping-criteria`, `verification` |
| 2026-06-05 | `freeze_after_k` OSGS mode — final form: k *single-step* warm-up iterations that refresh π=Π(R(u)) at every nonlinear iteration, then FREEZE π and a full quadratic frozen-π Newton finish; the projection is ALWAYS applied (no ASGS fallback). Two design missteps en route, both worth recording. | (1) **Wrong scheme first:** I built the warm-up as k *fully-converged* staggered solves (re-project between complete Newton solves), but the intended scheme is "project π at every nonlinear iteration for k iters, then freeze" = k *single* Newton steps (project each), then one frozen finish — leaner and the faithful reading of "every iteration". (2) **Wrong gate norm:** an interim ASGS-fallback gate used the **L²** relative state-drift `‖ΔU‖_{L²}/‖U‖_{L²}`; but in the reaction-dominated corner (Da=1e6, low Re) the staggered map still *contracts* (toward a fixed point worse than ASGS — a contraction-ratio test is blind), and the blow-up is in the **gradient**, not L² (measured: first-update L²-relative drift ~0.012-0.02, *smaller* than the benign cell's ~0.26 — anti-correlated; H¹ drift ~2-3). | (1) Warm-up = k single Newton steps via the existing cascade with `max_iters=1`, re-projecting between — because π is frozen *within* each step, each step is a valid Newton step (`D=−2Φ` holds), so no two-phase `:picard`/`:newton` line search is needed; the freeze+finish is a separate full Newton solve (exact tangent ⇒ quadratic). (2) The H¹-semi-norm gate `‖∇ΔU‖/‖∇U^0‖` (encoding-invariant) *did* correctly fire only in the defect corner — but it was **removed at the user's direction** ("always do the projection in all cases"): the mode now always projects and lets the reaction-dominated cells show OSGS's own sub-optimal rate honestly (does NOT mask the caveat-#5 formulation defect behind ASGS). Single knob `osgs_freeze_after_k` (k≥1) via schema+config+validate!+both harnesses; pinned by `freeze_after_k_config_blitz_test.jl`; encoding-covariance pinned by the freeze case in `encoding_invariance_quick_test.jl`. | `src/solvers/porous_solver.jl` (`freeze_after_k` branch), `src/config.jl`, `config/{base_config,porous_ns.schema}.json`, `test/extended/*/run_test.jl`, `test/blitz/freeze_after_k_config_blitz_test.jl`, `test/quick/encoding_invariance_quick_test.jl` | `OSGS`, `freeze_after_k`, `coupled-then-freeze`, `H1-norm`, `encoding`, `covariance`, `rate-vs-constant`, `always-project`, `verification` |
| 2026-06-07 | OSGS solver leaned to a SINGLE nonlinear route (`coupled`); the `staggered` + `freeze_after_k` branches and their H3–H6 satellite helpers were deleted. The clean-slate synthesis had recommended KEEPING `freeze_after_k` (fast quadratic finish), but a coupling-equivalence oracle gated on BEFORE deletion proved `freeze_after_k=2` is NOT equivalent to the OSGS fixed point: bit-fine in the healthy corner but DIVERGENT in the reaction-dominated corner (Re=1, Da=1e6, α=1: H¹(u) 1.17/1.28/1.62 at N=10/20/40, local rate −0.14/−0.33). Two warm-ups freeze a wildly-unconverged π (π_p needs ~17 steps to settle there); the frozen-π finish then drives U to a wrong solution that WORSENS under refinement. | Kept `coupled` (re-projects π=Π(R(u)) every iteration → always tracks the true fixed point; fewest parts). Deleted the other two branches + `_compute_state_drift`/`_update_and_project!`/`_decide_osgs_convergence`/`_run_osgs_inner_cascade!` (porous_solver.jl 1590→954 LOC). Permanent regression gate: `coupling_equivalence_extended_test.jl`. coupled's high-Re cost (44–69 iters = the dropped dense ∂π/∂U) is the deferred JFNK target. Blitz 189/189 after each stage. | `src/solvers/porous_solver.jl`, `test/extended/coupling_equivalence_extended_test.jl`, `docs/solver/coupled-only-leaning-and-jfnk-plan.md` | `OSGS`, `coupled`, `freeze_after_k`, `legacy-branch`, `verification`, `refactoring`, `leaning` |
| 2026-06-08 | Enabling the Stage-I Newton stall sensor (`newton_stall_window` / `solver_stall_window`) GLOBALLY via `base_nls`, so it also rode onto the Stage-II coupled OSGS solve | The coupled inexact-Newton converges SLOWLY-MONOTONE (linear rate — the dropped dense ∂π/∂U term). The stall sensor ("best residual fails to improve by `stall_min_rel_improvement` over `stall_window` consecutive steps") mistook slow-but-monotone progress for a stall and bailed the coupled solve after ~2 steps at high-Da/fine-mesh; `OSGS_INNER_POLICY` then ACCEPTED the `no_progress_stall` as success → U_h never left the ASGS state → **OSGS silently degenerated into ASGS**, reporting ASGS's optimal H¹ rate (~1.0) under the OSGS label at ‖R‖≈3. Invisible to Blitz/Quick (both passed); only the MMS rate exposed it: at Re=1,Da=1e6,α=1 the OSGS H¹ errors became bit-identical to ASGS at N≥40 (0.07789/0.03732/0.01822), iters collapsed to ~5, and the H¹ rate jumped from the true ~0.55 to a fake ~1.0 — which would have corrupted the entire high-Da OSGS rate study and falsely 'refuted' the coercivity-gap theory. | Added `stall_window`/`stall_min_rel_improvement` overrides to `_with_overrides`; the coupled solve builds its Newton (and Picard-fallback) solvers with `stall_window=0`, disabling the stall bail. Its genuine failures (line-search failure, divergence, max-iters) are still caught; the Stage-I boot keeps the stall sensor (cold-start oscillation → Picard). Verified: OSGS rate restored to ~0.55 (bit-identical to the old multi-route archive 0.9061/0.61/0.4197/0.2829), coupled solve iterates to `ftol_reached` (36 steps at N=40, not 2). Blitz 189/189 + Quick 42/42. | `src/solvers/nonlinear.jl` (`_with_overrides`), `src/solvers/porous_solver.jl` (coupled branch) | `OSGS`, `ASGS`, `stall-guard`, `coupled`, `stopping-criteria`, `MMS`, `regression`, `fix-applied`, `verification` |
| 2026-06-30 | **OSGS silently records the ASGS state via the `initial_ftol` short-circuit (recording leak).** When OSGS runs after the ASGS Stage-I boot (the default, non-boot-skip path), it enters the coupled solve already at the ASGS root. `nonlinear.jl:687` then short-circuits in **0 iterations** (`stop_reason="initial_ftol"`), `cascade_step_outcome` (`solver_core.jl:322`) maps that to `:success`, and `solve_osgs_stage!` returns `success=true` with `final_x0` UNCHANGED = the ASGS state. A comparison harness (CocquetFormMMS) recorded that as a genuine OSGS datum → byte-identical OSGS≡ASGS. | The numeric value isn't wrong (the boot state IS an OSGS root to the loose fold gate), but recording a 0-iteration point byte-identical to ASGS as a real OSGS datum is **misleading in an ASGS-vs-OSGS convergence study** — OSGS never ran. The existing `stall_window=0` guard (`osgs_solver.jl:366`) covers only the *stall* route to this degeneration; the `initial_ftol` route was a separate, unguarded one. Downgrading `success` in production `solve_system` would be wrong (the solve genuinely converged), so the guard belongs at the recording layer. | **Honest-recording guard (general).** `solve_osgs_stage!` now records, path-agnostically, whether the OSGS coupled stage ADVANCED off its entry iterate: `diag_cache["osgs_advanced_off_entry"]` (an exact DOF-vector inequality — a true short-circuit leaves `final_x0` bit-identical, so no tolerance), `osgs_stage_iters`, and `osgs_short_circuited_on_entry = success && !advanced`. The comparison harness (`CocquetFormMMS/run_test.jl`) consults it: a non-advancing OSGS "success" is recorded as **NaN** with a new `osgs_no_advance` flag (and skips the fold-retry, which would re-short-circuit), instead of the leaked ASGS value. Verified Blitz 240/240 (diag keys inert to computation). The 3D harness is unaffected (OSGS runs boot-skip). | `src/solvers/osgs_solver.jl`, `test/extended/CocquetFormMMS/run_test.jl` | `OSGS`, `ASGS`, `recording-leak`, `initial_ftol`, `boot`, `verification`, `provenance`, `fix-applied` |
| 2026-07-08 | **The frozen-π OSGS coupled solve is linear-rate and DOMINATES sweep wall-clock — enable JFNK for OSGS-heavy sweeps.** | The coupled inexact-Newton drops the dense `∂π/∂u`, so it converges *linearly*: measured 50–95 iters/cell on the CocquetFormMMS N=160 k=1 OSGS cells (25–45 min each) vs 3–7 iters for every ASGS cell (~28 s/iter for both). At k=2/N=160 that projects to hours/cell and is the entire sweep tail. | Set `osgs_jfnk_enabled=true` in the official sweep config (committed `40063fc`): matrix-free full-tangent inner GMRES recovers the dropped `∂π/∂u`, **same root, ~10–15× fewer outer iters** (verified in-flight: config_1_OSGS N=10 124→9, N=160 95→8 iters / 45→9 min ⇒ 5× wall-clock; errors identical). Verified-equivalent (`jfnk_equivalence_extended_test.jl`); keep `numerical_epsilon=null` (Phase-0-preferred for the preconditioner). | `test/extended/CocquetFormMMS/data/cocquet_form_mms_vms.json`, `src/solvers/osgs_solver.jl`, `docs/solver/jfnk-phase0-preconditioner-gate.md` | `OSGS`, `JFNK`, `coupled`, `linear-rate`, `sweep-efficiency`, `CocquetFormMMS`, `verified-equivalent` |

## 3. Newton/Picard solver, linear algebra & efficiency

Exact-Newton derivative terms, line-search/trust-region bounds, null-result conditioning experiments, cascade scheduling.

| Date | Error / Anti-pattern | Why it was wrong | Correct action taken | Related files | Tags |
| ---- | -------------------- | ---------------- | -------------------- | ------------- | ---- |
| 2026-04-13 | Silently dropping analytical derivative terms evaluating Exact Newton components | Fails to incorporate the $\partial_{\boldsymbol{u}} \tau$ and $\partial_{\boldsymbol{u}} \Pi$ functional evaluations, truncating true quadratic convergence to linear steps. | Mapped analytical discrete variations through explicit `ExactNewtonMode()` typing dispatch across stabilization kernels. | `viscous_operators.jl` | `Jacobian`, `Newton`, `adjoint-sign` |
| 2026-04-13 | Bounding Newton limits blindly using scalar dimensionless values | Initializing ALM bounds unconditionally truncates natural quadratic steps. | Implemented strict Trust-Region evaluations mapping element-wise scalar checks validating step reductions before committing. | `nonlinear.jl` | `Newton`, `stopping-criteria` |
| 2026-05-28 | Hypothesised that symmetric-Jacobi pre-scaling of the Newton Jacobian (D⁻¹AD⁻¹, D[k] = √\|A[k,k]\|) would recover slope at high-σ regimes (Da·α/Re up to 10¹²) — motivated by the OSGS failure cluster in `merged_convergence_report.md` (slope sub-optimal at σ ~ 10⁵–10⁶, folds at σ ~ 10¹⁰–10¹²) | Tested on two independent probes and the result was bit-identical L²(u) at the finest mesh in BOTH cases. (i) ASGS C23 (Re=10⁻⁶, Da=10⁶, α=0.05, σ ≈ 5·10¹⁰) at N=80, N=160: L²(u) identical to 7 sig figs, slope 1.741 unchanged. (ii) OSGS C16 (Re=10⁻⁶, Da=10⁶, α=0.5, σ ≈ 5·10¹¹) at N=40, N=80 via `solve_system` orchestration: same bit-identical result. UMFPACK's partial-pivoting LU already absorbs the row-scale disparity internally, so symmetric Jacobi pre-scaling becomes a mathematical no-op for this direct solver. The non-flat slopes and the OSGS folds therefore are NOT in the Newton linear-solve precision. Likely homes for the actual bottleneck: (a) homotopy outer loop's initial-guess basin (the production `eps_pert=1.0` first attempt lands in a noise-floor pseudo-root that the staggered loop can't escape — eps_pert=0 in my probe converged where the production sweep folded), (b) the OSGS L²-projection of the strong residual evaluated on a near-precision-floor u_h (differential operators on ‖u‖ ~ 1/σ amplify FP noise into π_h), (c) intrinsic discretisation noise floor at extreme parameters that no linear-algebra fix can address. | (Reverted.) Removed the `jacobian_equilibration` schema knob, the `equilibration_mode` field on `SafeNewtonSolver`, and the `_apply_symmetric_jacobi_scaling!` helper. Two diagnostic probes stay under `test/extended/ManufacturedSolutions/diagnostics/` (`jacobian_equilibration_probe.jl` for ASGS, `jacobian_equilibration_osgs_probe.jl` for OSGS) — they are the reusable record of the negative result and remain runnable if anyone revisits the conditioning hypothesis. Future attempts at high-σ slope recovery should NOT start with Jacobian row scaling; the leverage points are upstream (initial-guess strategy, OSGS projection regularisation, or a regime-aware re-non-dimensionalisation that keeps absolute term magnitudes within the FP safe zone [10⁻¹³, 10¹³] — only the last actually changes what the assembled residual sees). | `src/solvers/nonlinear.jl`, `src/config.jl`, `porous_ns.schema.json`, `base_config.json`, `src/run_simulation.jl`, `test/extended/ManufacturedSolutions/run_test.jl` (all reverted); `test/extended/ManufacturedSolutions/diagnostics/jacobian_equilibration_probe.jl`, `test/extended/ManufacturedSolutions/diagnostics/jacobian_equilibration_osgs_probe.jl` (kept) | `Newton`, `linear-solve`, `equilibration`, `UMFPACK`, `MMS`, `null-result`, `OSGS`, `high-sigma` |
| 2026-06-04 | Nonlinear-iterator optimization (`docs/solver/efficiency-ideas.md` Ideas 2/3/4 + refactor-brief B5/B6/B10): shared solver-builder (P0), explicit cascade policy (P3a), stall guard (P1), OSGS plateau machine-floor short-circuit (P2), adaptive Newton↔Picard ping-pong (P4). Three non-obvious traps cost real time and are worth recording. | (1) **`_with_overrides` silently drops new `SafeNewtonSolver` fields.** The OSGS inner Picard is rebuilt every outer iteration via `_with_overrides(base_nls; …)`; a new struct field (`picard_gain_target`) that is NOT threaded through that helper defaults to its struct value on the OSGS path, silently making P4's ping-pong a no-op there — exactly the "#1 way this task silently does nothing" the spec warned about. (2) **`base_config.json` is not directly loadable into a full `PorousNSConfig`** — it intentionally omits `eps_val` (so it can't be silently inherited; `lessons_learned` 2026-05-26). A P1 attempt to source the stall-guard default via `load_config_with_base_template()` therefore threw `Cannot convert Nothing to Float64` at `PhysicalProperties`. (3) **A bash `;` after a Julia run masks its nonzero exit**, so a *failed* harness run leaves a STALE h5 that a subsequent bit-identical check reads and falsely passes (the probe configs use `erase_past_results=true`, which only erases on a *successful* run). | (1) Thread `picard_gain_target` through `_with_overrides` (and assert defaults inert); every behavioural switch defaults OFF (`pingpong_enabled=false`, `newton_stall_window=0`, `osgs_plateau_machine_floor_shortcut=false`, `picard_gain_target=Inf`) so the shipped config is bit-identical — verified per phase (Blitz, Quick incl. `encoding_invariance_quick_test`, probe_k1 vs the covariant-complete snapshot). (2) Reverted the P1 stall default to an explicit literal `0` (= the canonical `SolverConfig.newton_stall_window`), documented; production sources it from `SolverConfig` via the shared builder. (3) Verify the Julia exit (`&& echo OK || echo FAIL`) AND delete the target h5 before every A/B run. Two further invariants: the cascade success-policy needs THREE flags (`accept_noise_floor`, `accept_soft_stall`, `max_iters_caught_is_failure`) — Stage-I N2 accepts a noise-floor finish but rejects soft stalls while OSGS-inner accepts both (a 2-flag design silently desymmetrizes N2); and P4 ping-pong on the OSGS inner solve only does real work when `osgs_inner_newton_iters > 1` (a 1-step inner Newton exits as an accepted soft-stall on the first segment). Stiff A/B (Re=Da=1e6, α=0.5): ASGS total iters 31→23 (−26%), OSGS 70→62 (−11%), errors and MMS rates bit-identical. | `src/solvers/nonlinear.jl` (`build_iter_solvers`, `picard_gain_target` + gain-stop, `_with_overrides`), `src/solvers/porous_solver.jl` (`CascadePolicy`/`cascade_step_outcome`/`_pingpong_cascade!`, P2 short-circuit), `src/config.jl` + `config/{base_config,porous_ns.schema}.json` (6 off/inert knobs), `test/extended/ManufacturedSolutions/run_test.jl` (shared builder call site, stall default), `test/blitz/{cascade_policy_symmetry,stall_guard}_blitz_test.jl` | `Newton`, `Picard`, `ping-pong`, `stall-guard`, `OSGS`, `cascade-policy`, `config-strictness`, `bit-identical`, `verification`, `efficiency` |

## 4. Encoding, scale-covariance & convergence gates

The discrete OSGS solve is not encoding-invariant unless the gates are; dimensional inputs must scale; k≥2 needs a tighter gate.

| Date | Error / Anti-pattern | Why it was wrong | Correct action taken | Related files | Tags |
| ---- | -------------------- | ---------------- | -------------------- | ------------- | ---- |
| 2026-06-02 | Switched the MMS sweep's default `encoding_strategy` from `centered` to `minmax` (motivated by `centered`'s `U_amp ~ 1e9` blow-up at Re=1e6) **assuming it was a benign, physics-preserving re-non-dimensionalisation**. It is not benign for OSGS at low-Re/high-Da. | The VMS-OSGS discrete solution is **not encoding-invariant** at this regime, so changing the units silently changed the answer. Controlled A/B on **Re=1, Da=1e6, α=1, OSGS** (only `encoding_strategy` varied; same eps_pert, stall_window, ftol; N≤160): **`centered` converges OPTIMALLY** (L²(u) rate 2.01→2.04, H¹(u) 1.01→0.91) while **`minmax` STAGNATES** (L²(u) 1.57→0.89, H¹(u) 0.58→0.17 — reproduces the main-sweep C18 numbers 7.99e-4 / 2.51e-1 exactly). The dimensionless problem (Da, Re, α, manufactured solution) is identical between the two runs, so this is a genuine encoding-dependence of the stabilised DISCRETE solve, not physics. **The cause is NOT a change in the τ₁ balance** — that was a mistaken first hypothesis (it used `h=1/N` instead of the physical `h=L/N`; the harness meshes the L-scaled domain, `_build_local_mesh` + `run_simulation.jl:327`). The two added terms in `1/τ₁ = α·c₁ν/h² + σ` share units (rate, 1/T), so their ratio `σh²/(c₁ν) = Da·α∞/(c₁N²)` is **dimensionless and invariant** under the encoding (≈9.8 at N=160,Da=1e6 for BOTH encodings; the dimensionless `τ₁·U/L` is invariant too). Confirmed empirically: the dimensionless errors are **identical at coarse meshes** (N=10: 3.86e-2 vs 3.84e-2; N=20 to 0.2%) and only diverge as the mesh refines (N≥40). That fine-mesh-only split is the fingerprint of an **absolute, scale-NON-invariant numerical floor**, not a balance/physics change. Mechanism (pinned by trace instrumentation — see `mms_next_actions.md` (since removed) §3a/§3b; this SUPERSEDES intermediate "balance-flip", "dimensional gate", and "FP noise-floor" guesses, all wrong). It is a **scale-coupled inner-Newton gate × a 1-iteration inner-Newton cap** — NOT floating point. (a) The per-field gate `‖R_k‖ ≤ rel·‖x_k‖` normalises the residual by `‖x‖∝U`, so `eff_ftol ∝ U`: measured 3.9e-10 (centered) vs 1.3e-8 (minmax), ratio ≈33 ≈ U-ratio 31.6. (b) `osgs_inner_newton_iters≈1` caps the inner solve at one step. Stop-reasons make it undeniable: **centered** inner solves fire `initial_ftol`/`ftol_reached` (≤1 step, residual→machine 6.5e-15) — the projection correction is BELOW the gate so OSGS silently degenerates to ASGS → optimal (so "centered OSGS optimal" is partly an artifact — it isn't doing OSGS). **minmax** inner solves fire `max_iters_stagnation` — residual ABOVE the gate, the one allowed Newton step lands ~17000× above it (`res_out_norm`≈1.7e4), so every outer iteration feeds an under-converged state to the projection → outer drift floors at ~1e-4 and oscillates → stagnant. The ~1e-4 floor is a single-step/under-convergence level (~12 orders above machine ε), NOT roundoff. The `τ₁` formula is fine (already unit-invariant). The leak is entirely in the GATES being scale-coupled; the encoding flips inert-ASGS vs engaged-but-capped via the `‖x‖∝U` normalisation. NOTE: this **corrects** two earlier conclusions — (a) the 2026-06-02 diagnosis-workflow regression verdict of "pre-existing / encoding-independent" was wrong (its "bit-identical" evidence was minmax-vs-minmax: the `_archive_preNorm` baseline was itself minmax); (b) `mms_convergence_status.md` caveat #2's "eps_pert=1.0 noise-floor pseudo-root" explanation is incomplete for low-Re high-Da cells — the SAME eps_pert=1.0 reaches the optimal basin under `centered`, so the basin's escapability is encoding-controlled here. | **No revert yet — policy decision pending** (see `docs/mms/next-actions.md`, since removed). The A/B leaves a `centered` config at `test/extended/ManufacturedSolutions/data/abtest_centered.json` and scratch DBs `results/abtest_{centered,minmax}.h5`. Two-part fix (see `mms_next_actions.md` (since removed) §3a/§3b): (1) **lift the ~1-iteration inner-Newton cap** (`osgs_inner_newton_iters`) so each OSGS inner solve actually converges — this fixes the rates AND reveals the true OSGS contraction at this regime (currently hidden by centered's inert/ASGS degeneration); (2) **make the per-field gate scale-invariant** — normalise the residual by a unit-free scale (operator scale or `‖R(x_0)‖`) instead of `‖x‖∝U` (`src/solvers/nonlinear.jl`), and likewise the OSGS outer state-drift gate (`porous_solver.jl:221`/`299`). The `τ₁` formula needs no change (already unit-invariant). Interim encoding policy (revert-to-centered vs per-regime) is a workaround, not the fix. | `test/extended/ManufacturedSolutions/run_test.jl` (`compute_L_and_U`, `encoding_strategy` default ~L481), `data/phase1_quad_k1.json` / `phase1_quad_k2.json` (`encoding_strategy`), `src/stabilization/tau.jl` (`compute_tau_1`) | `OSGS`, `MMS`, `encoding`, `non-dimensionalisation`, `tau`, `projection`, `high-sigma`, `regression`, `verification` |
| 2026-06-02 | The MMS harness injected `eps_val` (the continuity pressure-penalty coefficient in `eps_val·p`) as a FIXED `1e-8` for every `(L,U)` encoding (`build_mms_formulation`: `eps_calculated = config.physical_properties.eps_val`). But `eps_val` is **DIMENSIONAL** and must scale with the encoding. | In the continuity residual `eps_val·p + ∇·(αu) = g`, `∇·(αu) ∼ U/L` and `p ∼ P_c`, so `[eps_val] = (U/L)/P_c` — it must be derived per-cell exactly like `ν = U·L/Re` and `σ = Da·α∞·ν/L²`. A fixed value makes the **dimensionless** penalty `ε̂ = eps_val·P_c·L/U` encoding-dependent (≈1.9× between two test encodings), so the two encodings solve slightly different dimensionless problems and the discrete solution is **not scale-covariant**. Quantified by the new `encoding_invariance_test.jl` (Re=10, Da=2, α=0.5; centered vs minmax): with the fixed `eps_val`, **ASGS** dimensionless errors differed by 3.6e-6 (u L²) / 1.2e-6 (p L²); with the covariant `eps_val`, **1.0e-10 / 1.5e-10** — ~3·10⁴× better, restored to roundoff. The user's diagnosis was exact: "eps_val is a dimensional quantity that must be correctly inputted." | **Fixed** in `build_mms_formulation`: treat the config `eps_val` as the dimensionless penalty `ε̂` and derive `eps_calculated = ε̂·(U_amp/L)/P_c` per cell, with `P_c = (1+Re+Da)·U·ν/L` (`get_characteristic_scales`, `src/problems/mms_paper_2d.jl`). Guarded by `encoding_invariance_test.jl` (ASGS must hold ≤1e-8). **Scope:** this fixes the BASE-discretisation covariance (ASGS now covariant to ~1e-10). **OSGS still violates covariance independently** (u L² ≈4e-4, p L² ≈2e-2 — UNCHANGED by the eps_val fix): a separate staggered-loop/projection defect. IMPORTANT — that OSGS defect is NOT the prior entry's "scale-coupled gate × 1-iteration cap": that proposed fix was **tested 2026-06-02 (unit-invariant gate + inner cap 1→10) and did NOT cure it** — minmax's OSGS outer staggered loop still oscillated (rate 0.89, 11× worse) while centered converged. So the OSGS covariance defect is in the discrete staggered map (`Π(R(U(π)))` / stabilised assembly), still open. Single-run path (`run_simulation`) still uses a fixed `eps_val` — harmless there (no encoding sweep) but should be made dimensional for consistency. | `test/extended/ManufacturedSolutions/run_test.jl` (`build_mms_formulation`), `test/extended/ManufacturedSolutions/encoding_invariance_test.jl` (new guard), `src/problems/mms_paper_2d.jl` (`get_characteristic_scales`/`P_c`) | `MMS`, `encoding`, `non-dimensionalisation`, `eps_val`, `pressure`, `covariance`, `fix-applied`, `verification` |
| 2026-06-02 | OSGS-only scale-covariance violation (pressure-worst: encoding reldiff u-L²≈4e-4, **p-L²≈2.3e-2**), surviving the eps_val fix. With `osgs_inner_newton_iters=1` (single-step staggering) the inner Newton's STOP was set by two ENCODING-DEPENDENT inequalities (found via the user's "look in the inequalities" heuristic): (a) the per-field gate `‖R_k‖ ≤ rel·‖x_k‖` normalised by the SOLUTION magnitude `‖x_k‖` (∝U velocity, ∝P_c∝U² pressure), and (b) the ABSOLUTE warmup tolerance `‖R‖ ≤ osgs_warmup_tolerance=1e-3` (first 2 outer iters). | `‖R_k‖/‖x_k‖ ≤ rel` compares a residual to a solution-scaled bound ⇒ DIMENSIONAL/encoding-dependent; the warmup `‖R‖≤1e-3` is a fixed number vs a `∝U²` residual. So the inner solve stops at an encoding-dependent precision; with one inner step it is never re-converged ⇒ the staggered iterates (and the converged OSGS result) are non-covariant, worst in pressure (∝U²). ASGS immune (single solve to machine overshoots both gates). Isolation: making the OUTER state-drift gate relative changed NOTHING (it only governs early-exit; loop runs its budget) ⇒ REFUTED the outer-gate hypothesis; inner cap 1→30 ⇒ covariant 1e-10; covariant inner gate + `osgs_warmup_iterations=0` at inner=1 ⇒ covariant (u-L² 4e-4→2.3e-10, p-L² 2.3e-2→5.5e-10). | **Fixed.** (a) per-field gate normalised by the INITIAL residual `‖R₀_k‖` (dimensionless relative reduction `‖R_k‖ ≤ rel·‖R₀_k‖`, frozen at iter 0; removed the `‖x‖` scale + its per-iteration refresh, `src/solvers/nonlinear.jl`); (b) the WARMUP made covariant — `osgs_warmup_tolerance` reinterpreted as a DIMENSIONLESS relative-reduction target applied during the warmup outer iters (`‖R_k‖ ≤ osgs_warmup_tolerance·‖R₀_k‖`, via a new `relative_ftol_per_field` override in `_with_overrides`, `nonlinear.jl`/`porous_solver.jl`), instead of the absolute `max(ftol, osgs_warmup_tolerance)` ftol floor — so the warmup's early-relaxation robustness is RETAINED, now encoding-invariant. Warmup stays ENABLED (`osgs_warmup_iterations=2`). Guard: `encoding_invariance_test.jl` asserts BOTH methods ≤1e-8 with the warmup ENABLED (13/13; OSGS ~1e-10). Blitz 133/133, Quick 10/10 (OSGS errors bit-identical — absolute-ftol path untouched, since the relative warmup override is `nothing` when `relative_ftol_per_field` is unset). SEPARATE/STILL-OPEN: high-Da OSGS *rate* stagnation (Re=1/Da=1e6) is a convergence-rate issue, not covariance. | `src/solvers/nonlinear.jl`, `data/phase1_quad_k1.json` / `phase1_quad_k2.json` (`osgs_warmup_iterations=0`), `test/extended/ManufacturedSolutions/encoding_invariance_test.jl` | `OSGS`, `MMS`, `encoding`, `covariance`, `convergence-gate`, `inequalities`, `warmup`, `pressure`, `fix-applied`, `verification` |
| 2026-06-27 | **k≥2 (high-order) MMS needs a TIGHTER convergence gate than k=1** — the k=1 default `eps_tol_momentum=1e-6` stops high-order/high-Re cells early at a 5–10× worse solution. | The scale-free gate stops on `ε_M=‖momentum residual‖/D_M ≤ tol_M`. At `tol_M=1e-6`, k=2 cells finish above the discretization error, collapsing the finest-segment rate (e.g. k=2 L²u 160→320 rate 3.3→1.7, pressure first). Over-tightening breaks too (1e-12 → NaN). | Set `eps_tol_momentum=1e-9` (+ `ftol=1e-12`) for k=2 sweeps to recover `O(h³)`/`O(h²)`; leave the mass gate at its Route-B algebraic `ε_C`. This is the FIRST thing to check when "high-order/3D OSGS loses optimal convergence" — suspect the gate before the discretization or the solver. | `test/extended/ManufacturedSolutions/data/*k2*.json`, `docs/mms/convergence-2d.md` §1 | `MMS`, `high-order`, `k2`, `convergence-gate`, `eps_tol_momentum`, `rate` |

## 5. MMS harness: robustness, dispatch & provenance

Homotopy/perturbation policy, per-method config dispatch, and honest recording of what actually converged.

| Date | Error / Anti-pattern | Why it was wrong | Correct action taken | Related files | Tags |
| ---- | -------------------- | ---------------- | -------------------- | ------------- | ---- |
| 2026-04-13 | Artificially weakening MMS iterative plateaus to circumvent poor solver iterations | Falsely accepts initial noisy residuals as analytically sound "convergence," masking underlying physical formulation mapping regressions. | Mandate strict multi-pass sequence evaluation $r_{\text{max}} \le \tau_{\text{error}}$ across iterative mesh refinements. | `ManufacturedSolutions` | `MMS`, `verification` |
| 2026-05-26 | Attempted "fix" for sub-optimal slope at extreme-parameter MMS cells by reversing the eps_pert iteration order (hard → easy → easy → hard) | **WITHDRAWN by author 2026-05-26.** Diagnostic was correct: a velocity-centering probe (`diagnostics/velocity_centering_probe.jl`) showed config_18 (Re=Da=10⁶, α=0.5) reaches a 30× tighter residual and a ~2× lower L²(u) at N=160 when Newton starts from x₀=u_ex (eps_pert=0) versus from a perturbed guess (eps_pert=1.0). But the inference — that the original hard→easy order was a bug — misread the harness's design intent. The default-large-perturbation outer loop is a deliberate **robustness cascade**: the harness records in `eval_eps[n]` the largest perturbation that Newton absorbed at each mesh, giving an explicit measure of where the solver needed help. A code that only works when primed with the analytical exact solution is **not practical** — real-world use never has u_ex available. The sub-optimal slopes at config_18 / config_23 are therefore the solver's **honest behaviour from a generic initial guess** at those extreme regimes; they should be read as a measurement of solver-robustness limits, not as a bug in the MMS harness. | (Revert.) Restored the original hard→easy iteration order in both MMS harnesses. The velocity-centering probe stays in `diagnostics/` as a tool to characterise the noise-floor pseudo-root phenomenon if needed in the future — it is NOT the production solve path. If solver improvements (better preconditioning / variable rescaling / N-cascade x₀) ever raise the noise-floor ceiling for extreme regimes, this will show up as the largest-successful `eval_eps` migrating up at those cells. | `test/extended/ManufacturedSolutions/run_test.jl` (line ~161, homotopy loop — reverted), `test/extended/CocquetFormMMS/run_test.jl` (line ~230, reverted), `test/extended/ManufacturedSolutions/diagnostics/velocity_centering_probe.jl` (kept as diagnostic) | `MMS`, `homotopy`, `Newton`, `robustness`, `design-intent`, `withdrawn-fix` |
| 2026-05-26 | OSGS sweep rows in `phase1_quad_k1.h5` are mislabelled ASGS — outer-loop `method` variable never reaches the solver | The MMS sweep harness builds an inner `config_dict` per (etype, kv, kp, n) tuple with `"stabilization" => Dict("method" => "ASGS", …)` HARDCODED. The outer iteration `for method in methods` uses `method` only to (a) compute the HDF5 group key (`config_X_$(method)`) and (b) build a `local_stab_cfg` that is **never passed to `solve_system`**. `solve_system` reads the stabilisation method from `config.numerical_method.stabilization.method`, which equals "ASGS" for every solve. Detection symptom: ASGS and OSGS rows are bit-identical in `merged_convergence_report.md` across all 24 cells (slope, magnitude, total_iters, eval_residuals all match to full precision). Per CLAUDE.md "ASGS ≠ OSGS" invariant, identical results across regimes (including coarse N where the strong residual is O(1) and π_h(R_u) is non-trivial) is impossible if both branches are running. | **Fixed 2026-05-26.** Inside the per-method loop, the harness now builds `method_config_dict` = deepcopy(config_dict) with `stabilization` overridden to `{"method" => method, "osgs_iterations" => ..., …}` propagating ALL OSGS-specific fields from `test_dict["numerical_method"]["stabilization"]` (so the test JSON's osgs_iterations / osgs_tolerance / etc. actually take effect instead of being silently overridden by `base_config.json` defaults). `method_config = load_config_from_dict(method_config_dict)` is then passed to `execute_outer_homotopy_perturbation_loop!` → `solve_system` in place of the outer `config`. HDF5 cleanup: deleted all 23 `*_OSGS` groups from `phase1_quad_k1.h5` (backup at `phase1_quad_k1.h5.bak_before_osgs_fix`) and re-launched the sweep. The resume-aware skip leaves the 24 correct ASGS rows untouched; only OSGS is computed. | `test/extended/ManufacturedSolutions/run_test.jl` (lines ~630-665, per-method config_dict reconstruction). Latent in `test/extended/CocquetFormMMS/run_test.jl` (same hardcoded "ASGS" pattern) but not currently triggered since `CocquetFormMMS` configs only iterate methods ∈ {ASGS, Galerkin}; would need the same fix if "OSGS" is ever added to that test. | `OSGS`, `ASGS`, `dispatch`, `MMS`, `verification`, `fix-applied` |
| 2026-07-08 | **Fine-mesh high-Re MMS convergence is initial-guess-basin-sensitive and multi-threaded-BLAS-nondeterministic — a stalled eps=1.0 rung is NOT a regression.** | On the CocquetFormMMS Re=1e5 cells at N=160, the `epsilon_pert=1.0` (fully-perturbed) homotopy rung lands in a bad basin and floors at residual ~0.27 with line-search depletion, burning the full Newton budget before the ladder falls to eps=0.1 and converges in ~4 iters to the correct root. The frozen-π baseline converged the *same* cell at eps=1.0 in 4 iters — the difference is multi-threaded BLAS roundoff (non-deterministic reduction order) nudging this knife's-edge stiff cell onto the stall path. Every eps rung solves the same `F=0`, so the converged error is identical; only the `eval_eps` robustness fingerprint softens. | **Read it as robustness-ladder cost, not a bug** — the ladder's last rung (eps=0.0 = exact guess) is guaranteed to converge, so it always terminates with the correct result. Do NOT "fix" by trimming the ladder (that changes the recorded robustness fingerprint — see the 2026-05-26 withdrawn-fix row). Operational: don't run concurrent heavy `julia` jobs — 4× parallel solves thrashed the machine into swap (5.8 GB, ~13% effective throughput); monitor RSS on long sweeps. | `test/extended/CocquetFormMMS/run_test.jl` (homotopy loop, `eval_eps`) | `MMS`, `high-Re`, `homotopy`, `eps_pert`, `BLAS-nondeterminism`, `robustness`, `not-a-regression` |

## 6. 3D MMS: penalty well-posedness & the P2-3D coercivity saga

The iterative-penalty well-posedness fix, and the long (finally resolved) c₁/coercivity story for P2 on structured tets.

| Date | Error / Anti-pattern | Why it was wrong | Correct action taken | Related files | Tags |
| ---- | -------------------- | ---------------- | -------------------- | ------------- | ---- |
| 2026-06-24 | **Non-zero `eps_phys` default in the 3D MMS harness.** A working-tree edit set `solve_one`'s `eps_phys` (formulation `eps_val`) default to `1e-4·α₀/(ν(1+Re+Da)) ≈ 1.67e-5` instead of `0.0`. | The equation is INCOMPRESSIBLE. A non-zero `eps_phys` puts `ε·p` in the RESIDUAL **and** `ε·p_ex` in the manufactured oracle `g`, so the solver was driving a genuinely COMPRESSIBLE problem — not the paper's case (article.tex `eq:StrongMassEquation` line 239 "ε ≥ 0 … mainly for numerical reasons"; the 2D examples use ε=0, line 1098). The ONLY ε is the numerical iterative penalty `numerical_epsilon` (`eps_num`): Jacobian-only, lagged (ε·pⁿ⁻¹ to the RHS), CANCELS at convergence. Rate-irrelevant for P₁ (ε≈1.7e-5) but the wrong problem — and a plausible trigger for the P₂-structured convergence failures (it changes the mass-block conditioning). | Reverted `eps_phys` default to `0.0` (committed-correct) and documented the invariant inline in `solve_one`. Verified eps-insensitive for P₁ (structured P₁ rates identical to 4 digits, eps_phys 0 vs the buggy value). | `test/extended/ManufacturedSolutions3D/smoke3d.jl` | `eps_phys`, `eps_num`, `iterative-penalty`, `MMS`, `3D`, `incompressible`, `fix-applied`, `verification` |
| 2026-06-24 | **Was the 3D ASGS L²u sub-optimality a nonlinear-iteration / Jacobian / orchestration artifact, or a true discrete fixed point?** It persists on a *perfect* structured mesh (qmin≈0.618) where OSGS is optimal, so the iteration was a live suspect — given the 2026-06-08 stall-sensor regression precedent, and that production decides convergence via the dimensionless scale-free gate (`ε_M,ε_C`), not the raw residual. | If the solver were converging to a different/under-resolved object, the "sub-optimal fixed point" diagnosis (and the whole c₁/coercivity story) would be wrong. | Direct verification on structured (16,16,4) P₁ ASGS at c₁×1/×4: **[A]** the logged `f(x) inf-norm` IS the raw assembled `‖R(U)‖∞` (2.14e-7; it floors there = constant-pressure null mode / global-mass quadrature with `eps_phys=0`, ~5 orders below the 1e-2 discretization error); **[B]** Jacobian Taylor test `‖R(U+εδU)−R(U)−εJδU‖` drops ~100×/decade (O(ε²) to round-off) ⇒ the analytic ExactNewton Jacobian IS the exact ∂R; **[C]** a bare Newton (analytic J, plain LU, NO Picard/homotopy/verifier/scale-free gate) from the interpolant lands on production (velocity DOFs ~3e-6; MMS errors identical). And c₁×1 L²u 0.0113 → c₁×4 0.0062 on the SAME mesh ⇒ c₁ moves the converged error. **Verdict: iteration + Jacobian + orchestration EXONERATED; the deficit is a coercivity-limited discrete fixed point (`c₁ > 2ξ C_inv²`), which c₁ — not the solver — changes.** | one-off diagnostic (residual-identity + Jacobian Taylor test + bare-Newton; method in `docs/mms/p2-3d.md` §4, driver not retained) | `ASGS`, `3D`, `coercivity`, `c1`, `fixed-point`, `Jacobian`, `Taylor-test`, `bare-Newton`, `nonlinear-iteration`, `verification` |
| 2026-06-30 | **3D P2 MMS "needs a dimension-aware c₁" — WRONG; it was a missing iterative-penalty term.** The 2026-06-24 conclusion (paper `c₁=4k⁴` under-budgets coercivity for 3D tets; c₁×4 fixes it) was a MASK. c₁ is dimension-independent and the paper's author confirmed the 3D case runs at paper c₁ in Kratos — so c₁×4 was over-stabilizing to paper over a real bug. | A "dimension-aware c₁" is not a real construct (no paper has it). Treating the c₁×4 workaround as the fix would have shipped an unphysical, regime-specific constant and never converged the actual issue. c₁×4 only shrank the error *constant* (not the rate), which is exactly how it masked the defect. | Found the real cause: the **Codina iterative penalty** (article.tex §5.2 line 1383) was never implemented in the mass RESIDUAL — only `ε_num·dp` was in the Jacobian (`mass_term_jac`), and the code comment "lagging ε_num·p cancels in the residual" wrongly equated `pⁿ⁻¹` (previous nonlinear iterate) with the Newton increment `dp`. The 3D all-Dirichlet problem is ill-posed at ε=0 (line 1375); 2D uses ε=0 so it never exposed the bug. **Fix (gated, default-OFF, Blitz 240/240):** `build_stabilized_weak_form_residual` `p_prev` kwarg adds `ε_num·(p−p_prev)` to the mass residual (matching the existing Jacobian term); `solve_system` runs an OUTER iterative-penalty loop (`iterative_penalty_enabled`); `p_prev` threaded into the OSGS coupled/Anderson closures. Verified: ASGS-3D converges at PAPER c₁ with optimal rate (L²u 5.25), converged solution unaltered (penalty vanishes at convergence). Also: the ASGS Stage-I boot is a code-side safeguard (not in `alg:StationarySystem`) and is HARMFUL for OSGS (lands at the ASGS root → overshoot) — `osgs_skip_asgs_boot` flag added. **OSGS-3D far-guess robustness OPEN** (dropped ∂π/∂u; see canonical doc). | `src/formulations/continuous_problem.jl`, `src/solvers/solver_core.jl`, `src/solvers/osgs_solver.jl`, `src/config.jl`, `config/base_config.json`, `config/porous_ns.schema.json`, `test/extended/ManufacturedSolutions3D/smoke3d.jl`; canonical doc `docs/mms/p2-3d.md` | `3D`, `MMS`, `iterative-penalty`, `OSGS`, `ASGS`, `c1`, `well-posedness`, `eps_num`, `boot`, `regression`, `fix-applied`, `paper-faithful` |
| 2026-07-03 | **3D-P2 c₁ verdict RE-REVERSED: the element-family c₁ coercivity deficit IS confirmed — the 2026-06-30 "it was a mask, not c₁" row is now wrong on P2 accuracy.** | The 2026-06-30 row correctly found + fixed the missing Codina iterative penalty (a real 3D all-Dirichlet WELL-POSEDNESS fix), but wrongly extended "c₁×4 was over-stabilizing to mask a bug / there is no dimensional c₁" to the **P2 converged-but-wrong accuracy defect**. That inference rested on a SINGLE-MESH c₁×4 A/B (where ×2 already looks healed) + "Kratos runs paper c₁". | The decisive test is the **mesh-FAMILY ratio-to-interpolant** (§5.1 c1_mult sweep, smoke3d.jl): c₁×4 PINS ratio→interp ~1 across the Kuhn ladder for BOTH ASGS and OSGS; c₁×2 DRIFTS (1.34→1.94, masks); paper c₁ is 20–95× the interpolant. So c₁ genuinely FIXES (not masks) — the paper `c₁=4k⁴` under-budgets `2ξ·C_inv²` for P2 **Kuhn tets** (element-family), reconciled with Kratos via a reduced high-order subscale + smaller quad/hex `C_inv`. The penalty→well-posedness fix is UNAFFECTED. c₁ multiplier confirmed effective but **NOT adopted** (author prefers a root-cause fix). NumPy clean-room committed. Two issues had been conflated: well-posedness (penalty) vs accuracy (c₁). | `docs/mms/p2-3d.md` (canonical — verdict flipped), `docs/formulation-audit-2026-06-24.md` §B.5/F5, `docs/convergence_problems_audit/` | `3D`, `MMS`, `c1`, `coercivity`, `element-family`, `ratio-to-interpolant`, `verdict-reversal`, `P2`, `correction` |
| 2026-07-05 | **3D-P2 c₁ verdict REVERSED AGAIN (authoritative — the paper's first author): c₁ is a MASK, not the cause.** The 2026-07-03 row above ("element-family c₁ coercivity deficit CONFIRMED") is WRONG. | The first author confirms **Kratos assembles the FULL subscale (no terms removed) and solves the exact 3D §5.2 P2 case OPTIMALLY at paper c₁=4k⁴ on tetrahedra** (ASGS & OSGS). So paper c₁ is CORRECT and the element-family-`C_inv` argument is **refuted** — the NumPy clean-room "C_inv exceeds 4k⁴" is circular (it was transcribed term-by-term from `continuous_problem.jl`, inheriting the discrepancy). | In Gridap, c₁ CONTROLS the P2-3D error (c₁×4: ~40× L²u collapse, ratio-to-interpolant → 1, even optimal asymptotic rates on the 4-mesh ladder) — but this only **MASKS a Gridap↔paper IMPLEMENTATION discrepancy** specific to P2-3D. Constrained to **P2-only** (P1 works) ∩ **3D-only** (2D-QUAD k=2 works) ⇒ most likely the **3D viscous 2nd-derivative subscale / grad-div coupling** `(½−1/d)∇(∇·u)` (0 in 2D, 1/6 in 3D), possibly broader. **ROOT CAUSE OPEN**: find + correct the code↔paper term-level discrepancy so paper c₁=4k⁴ works (as in Kratos) — do NOT ship a c₁ multiplier. Docs re-corrected (this reverses the 2026-07-03 row + the session's `instability-investigation.md`/`§B.5`/README/known_issues edits). | `docs/mms/p2-3d.md` (canonical), `docs/formulation-audit-2026-06-24.md` §B.5, `docs/convergence_problems_audit/` | `3D`, `MMS`, `c1`, `Gridap-vs-Kratos`, `implementation-discrepancy`, `verdict-reversal`, `P2`, `author-authoritative`, `mask-not-fix` |
| 2026-07-06 | **3D-P2 "converged-but-wrong" RESOLVED — `4k⁴` is under-margined for high-`C_inv` structured Kuhn tets (final verdict of the c₁ saga; supersedes the 2026-07-03/07-05 rows).** | The viscous 2nd-derivative subscale is **anti-coercive by construction** (self-adjoint viscous op ⇒ `B_S` carries `−τ‖𝐛_visc V‖²`), so coercivity needs `c₁ > 2ξ·C_inv²`. Paper `c₁=4k⁴` has ~zero margin for 3D tets (`C_inv²` Kuhn 214 vs Q2-quad 60, mesh-independent; Kuhn shape-reg `h/ρ=8.36`) → structured Kuhn sits **over the coercivity edge** → the 20–95× converged-but-wrong error. Proven two ways: (i) Céa — a 50× interpolant error forces `M/α≳50` ⇒ coercivity≈0 (a coercive form *cannot* give 50×, refuting "definite-but-buggy"); (ii) `c₁×4` restores a **monotone convergent** Kuhn ladder. Reconciles with the paper's optimal §5.2 (unstructured tets, only 3 meshes — never reached the resolution where structured reverts) and `article.tex` **line 910** ("optimal c₁ depends on element type via the inverse-estimate constant"). | **Verdict = element-aware `c₁`, NOT a code change.** The 2026-07-05 "Gridap↔paper discrepancy / c₁ masks a bug" framing (row above) is **WITHDRAWN**: c₁ is the theorem's own coercivity knob and `4k⁴` is simply an under-margined element-dependent choice. No c₁ multiplier shipped (paper c₁ stays default); the remedy is a documented element-aware c₁ per line 910. Caveat: well-shaped 3D tet families are hard to build (gmsh alg 11–13; BCC bulk 5.66/bdry 8.83 still erratic), so a clean positive test remains open. | `docs/mms/p2-3d.md` §A, `src/stabilization/tau.jl` (`TAU_VISC_MULT`), `test/extended/ManufacturedSolutions3D/smoke3d.jl` | `3D`, `MMS`, `c1`, `coercivity`, `C_inv`, `Kuhn`, `element-aware`, `P2`, `RESOLVED`, `verdict-final` |
| 2026-07-09 | **OSGS-P2-3D can't be solved at paper c₁ even though the ASGS root is fine — the frozen-π preconditioner is deficient, not the budget.** | On P2-3D, `ρ(J_frozen⁻¹·∂π/∂u)≈1249` at paper c₁ (2D≈0.88), so GMRES on the full tangent stalls and the solve is stuck at the exact-guess INTERPOLANT (the "accurate but `ok=false`" was interpolation error, not a reached root). A classic Schur preconditioner can't help — the preconditioner is already the exact `J_frozen⁻¹`; the deficit is the DROPPED `∂π/∂u`. | **Fix = preconditioner-ONLY `c₁×4` inflation** (`osgs_jfnk_precond_c1_mult`, default-off): `ρ_prec→3.8`, root unchanged (F stays paper-c₁, ‖F‖→1.4e-12), `success=true` + `eps_used=1` + quadratic. Refuted: gate / FD-noise / stronger-ε_num / gauge-deflation / damped-staggering. Exposes a separate fact: paper-c₁ P2-3D pressure is under-stabilized (L²p=0.045). Blitz 272 + Quick 85. | `src/solvers/osgs_solver.jl` (`_osgs_jfnk_solve!`, `osgs_jfnk_precond_c1_mult`), `docs/mms/p2-3d.md` §C | `OSGS`, `3D`, `P2`, `JFNK`, `preconditioner`, `dpi-du`, `fix-applied` |

## 7. Refactoring & config hygiene (behavior-preserving)

File splits, shared-core extraction, dead-config purges, and the no-implicit-defaults rule.

| Date | Error / Anti-pattern | Why it was wrong | Correct action taken | Related files | Tags |
| ---- | -------------------- | ---------------- | -------------------- | ------------- | ---- |
| 2026-05-26 | `eps_val` (pressure penalty η) silently inherited 1e-8 from `base_config.json` template for Cocquet paper-comparison configs | Violation of the CLAUDE.md "no implicit defaults — fail loudly on missing input" rule. The Cocquet paper specifies η = 1e-7, but most paper-comparison configs (`CocquetTubeTest/data/structured/paper_comparison.json`, `CocquetTubeTest/data/modified_corner/*`, `CocquetTubeTest/data/{alpha_one,deviatoric,linear_reaction,all_dirichlet}/*`) did not override the inherited default. The freefem-divs / literal-FreeFem-mesh / LiteralPicard configs DID override, so the headline magnitude-gap numbers were already paper-faithful. Empirically the change from 1e-8 to 1e-7 moves L²(u) by < 1e-10 (re-ran the canonical structured sweep post-retrofit and got ASGS P1/P1 bit-identical to 4 sig figs vs the pre-retrofit run). | Added `"eps_val": 1e-7` explicitly to all seven affected configs; removed `eps_val` from `base_config.json` so silent inheritance through `load_config_with_*` is impossible. Future Cocquet configs must declare `eps_val` or fail at struct construction (the `PhysicalProperties` @kwdef has no default for this field). | `base_config.json`, `test/extended/CocquetTubeTest/data/structured/paper_comparison.json`, `test/extended/CocquetTubeTest/data/modified_corner/*.json`, `test/extended/CocquetTubeTest/data/alpha_one/alpha1.json`, `test/extended/CocquetTubeTest/data/deviatoric/deviatoric.json`, `test/extended/CocquetTubeTest/data/linear_reaction/linear_reaction.json`, `test/extended/CocquetTubeTest/data/all_dirichlet/all_dirichlet.json` | `config-strictness`, `no-default`, `Cocquet`, `verification` |
| 2026-06-08 | **Code-debt cleanup after the coupled-only leaning.** The deleted `staggered`/`freeze_after_k` machinery left a dead *config surface*: **8** OSGS knobs (`osgs_inner_newton_iters`, `osgs_stopping_mode`, `osgs_projection_tolerance`, `osgs_state_drift_scale`, `osgs_warmup_iterations`, `osgs_warmup_tolerance`, `osgs_projection_coupling`, `osgs_freeze_after_k`) still in schema/struct/configs/harnesses, plus two tests of deleted behaviour: `freeze_after_k_config_blitz_test.jl`, and the coupling-equivalence oracle `coupling_equivalence_extended_test.jl` (now a coupled-vs-coupled tautology — with `freeze_after_k` gone there is no second route to compare, and its header still claimed "keeping ONLY freeze_after_k", the inverse of what shipped). | A solver-source trace confirmed `porous_solver.jl` reads NONE of the 8 — only `method`, `osgs_iterations`, `osgs_tolerance` are live; the 8 were inert plumbing the MMS/Cocquet harnesses forwarded into a `StabilizationConfig` the leaned solver ignores. `osgs_inner_newton_iters` was the widest straggler (11 CocquetFormMMS configs + `test_config.json` + a diagnostics probe + two more inline stab-dicts) and was only surfaced by a post-edit grep — the concrete value of verifying "is it actually dead" before deleting. The `_check_unknown_keys` loader only `@warn`s on unknown keys (it does not throw), so removal can't crash a half-updated config mid-flight. | Removed all 8 from `StabilizationConfig` + its `@assert`s, the JSON schema (8 property blocks, brace-matched + revalidated), `base_config.json`, both sweep configs, 11 Cocquet/`test_config` configs, both MMS-harness `StabilizationConfig` rebuilds + every per-method `method_stab_dict`, the OSGS-orthogonality/encoding-invariance quick tests, and the equilibration probe; deleted `freeze_after_k_config_blitz_test.jl` (unregistered from `run_blitz_tests.jl`) and the retired oracle; dropped the `freeze_after_k` covariance case from `encoding_invariance_quick_test.jl`; refreshed `porous_solver.jl`'s stale H3–H6 / staggered-dispatch docstrings. Verified: Blitz **183/183** (was 189; −6 = the removed freeze test), the OSGS-orthogonality + encoding-invariance quick tests **17/17** (scale-covariance reldiff ~5e-10/1e-9 ≤ 1e-8), Cocquet harness parses, all configs valid JSON. | `src/config.jl`, `config/{base_config,porous_ns.schema}.json`, `test/extended/{ManufacturedSolutions,CocquetFormMMS}/run_test.jl`, 13 `data/*.json`, `test/quick/{osgs_orthogonality,encoding_invariance}_quick_test.jl`, `test/run_blitz_tests.jl`, `src/solvers/porous_solver.jl` (docstrings); deleted `test/blitz/freeze_after_k_config_blitz_test.jl`, `test/extended/coupling_equivalence_extended_test.jl` | `OSGS`, `coupled`, `dead-code`, `config-strictness`, `cleanup`, `leaning`, `verification` |
| 2026-06-08 | **Solver simplification: ASGS/OSGS file split + MMS decoupling + dead-config purge.** Two judgement calls were load-bearing. | (1) A proposal floated WIRING the validated-but-ignored `osgs_iterations`/`osgs_tolerance` into the coupled solve. That is a ROBUSTNESS REGRESSION: the coupled inexact-Newton needs the full Newton budget (baseline 30–104 inner steps), but configured `osgs_iterations` is 3/5/15 — capping there truncates the descent and silently degenerates OSGS→ASGS (the exact failure the 2026-06-08 stall-sensor row documents). The per-field gate `max(ftol, rel·‖R₀‖)` also makes a scalar `osgs_tolerance` override inert in the MMS harness. (2) A struct-field doc comment on `relative_ftol_per_field` still described the OLD solution-scale gate (`‖x₀‖`), contradicting the code, which scales by the initial RESIDUAL `‖R₀‖` (the 2026-06-02 covariance fix). | **Part C = REMOVAL, not wiring** (number-neutral: the coupled solve already runs at the Newton budget/ftol; removal only cleans config/schema/doc). Split `porous_solver.jl` → `asgs_solver.jl` (core+ASGS+orchestrator) + `osgs_solver.jl` (`solve_osgs_stage!` + projections); decoupled the optional MMS plateau verification (Algorithm D) behind a no-op-default `SolutionVerifier` seam (`on_asgs_converged!`/`on_osgs_converged!`; concrete `MMSPlateauVerifier` in `mms_verification.jl`) — relocated VERBATIM so plateau outcomes are bit-identical. Removed dead code (Anderson *config* surface, `osgs_plateau_machine_floor_shortcut`, two Cholesky `numerical_setup!` methods, two `_with_overrides` kwargs) but KEPT the Anderson *implementation* (`accelerators.jl`, exported) as a future lever for the slow OSGS iterations. Renames: `IterativeSolvers`→`StageSolvers` (struct/package collision), `solution_scale_per_field`→`initial_residual_scale_per_field`; merit closure de-duplicated to `_merit_W`; residual-scale doc comment fixed. Verified per stage: Blitz **183/183**, Quick **36/36**, MMS smoke (ASGS+OSGS plateau established, H¹ rates ≈1.0 match baseline, no degeneration). | `src/solvers/{asgs_solver,osgs_solver,mms_verification,nonlinear,linear_solvers,accelerators}.jl`, `src/PorousNSSolver.jl`, `src/config.jl`, `config/{base_config,porous_ns.schema}.json` + 27 data JSON, both MMS harnesses, `docs/solver/algorithm-code-mapping.md` | `OSGS`, `ASGS`, `refactoring`, `file-split`, `MMS`, `observer-seam`, `dead-code`, `Part-C`, `Anderson`, `verification` |
| 2026-06-08 | **Shared-core extraction (commonality / per-method-difference split).** Follow-on to the 8b9f805 ASGS/OSGS split: `asgs_solver.jl` was *named* for ASGS but actually held ~75% shared core + orchestrator (the FE containers, the `SolutionVerifier` seam, the `CascadePolicy` machinery, `safe_fe_solve!`, and `solve_system`), so it still referenced OSGS (`method=="OSGS"` dispatch, the `on_osgs_converged!` no-op, `OSGS_INNER_POLICY`) — which read as an incomplete split. | An adversarial audit confirmed NO OSGS *algorithm* leaked into `asgs_solver.jl` (all projection / L² / coupled-solve math is in `osgs_solver.jl`); the problem was naming + cohesion, not stranded logic. Two facts were load-bearing: (1) `on_osgs_converged!` cannot move to `osgs_solver.jl` alone — it is one method of the `SolutionVerifier` seam named in `solve_system`'s default signature, so it must travel WITH the seam; only `OSGS_INNER_POLICY` was independently movable. (2) `solve_system` calls `_initialize_asgs_state!` / `solve_osgs_stage!` only in its BODY (resolved at call time, not parse time), so a new `solver_core.jl` can be `include`d BEFORE both method files even though it calls into them. | Extracted `src/solvers/solver_core.jl` = the commonalities: FE containers (`FETopology`/`VMSFormulation`/`StageSolvers`), the `SolutionVerifier` seam, the shared cascade (`CascadePolicy` + `cascade_step_outcome` + `_pingpong_cascade!`), the FE-solve plumbing (`safe_fe_solve!`/`_solve_one_step!`/`_record_stage!`), and the `solve_system` orchestrator. `asgs_solver.jl` reduced to ASGS-only (`_initialize_asgs_state!` + `STAGE_I_POLICY`/`STAGE_I_N2_POLICY`); `osgs_solver.jl` gained `OSGS_INNER_POLICY`. Each method file is now one Algorithm box + its own cascade-policy value (read side-by-side ⇒ the differences; `solver_core.jl` ⇒ the commonalities). PURE RELOCATION (number-neutral). Also fixed stale `src/solvers/porous_solver.jl` strings the 8b9f805 split missed (2 MMS/Cocquet harness `println`s + 1 blitz-test comment). Include order: `nonlinear → accelerators → solver_core → asgs → osgs → mms`. Verified: Blitz **183/183** (identical), Quick **36/36** (identical), Extended MMS spot-check (Re=1, Da=1, QUAD, k=1; ASGS+OSGS; N≤80) **bit-identical** to the pre-refactor baseline at every recorded (cell, N) — including the full plateau-verification path through the relocated seam. | `src/solvers/{solver_core (NEW), asgs_solver, osgs_solver, mms_verification}.jl`, `src/PorousNSSolver.jl`, `docs/solver/algorithm-code-mapping.md`, `docs/README.md`, `CLAUDE.md`, `test/blitz/cascade_policy_symmetry_blitz_test.jl`, `test/extended/{ManufacturedSolutions,CocquetFormMMS}/run_test.jl` | `refactoring`, `file-split`, `shared-core`, `separation-of-concerns`, `orchestrator`, `verifier-seam`, `include-order`, `number-neutral`, `verification` |
